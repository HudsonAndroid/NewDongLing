<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--标题栏的颜色-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--状态栏的颜色-->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--默认的光标、选中的RadioButton颜色-->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="TransparentTheme" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@color/common_transparent_activity_bg</item> <!-- 背景色透明度 -->
        <item name="android:windowNoTitle">true</item><!-- 无标题 -->
        <item name="android:windowIsTranslucent">true</item><!-- 半透明,设置为false无透明效果 -->
        <item name="android:backgroundDimEnabled">true</item><!-- 模糊 -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item> <!-- 窗口样式Dialog -->
    </style>

    <style name="common_guide_next_style">
        <item name="android:paddingLeft">@dimen/space_big</item>
        <item name="android:paddingRight">@dimen/space_big</item>
        <item name="android:paddingTop">@dimen/space_little</item>
        <item name="android:paddingBottom">@dimen/space_little</item>
        <item name="android:background">@drawable/shape_round_button_bg</item>
        <item name="android:textSize">@dimen/normal_text_size</item>
        <item name="android:textColor">@color/common_white</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">@string/guide_step_next</item>
    </style>

    <style name="item_menu_setting_style">
        <item name="android:textColor">@color/common_text_color</item>
        <item name="android:textSize">@dimen/normal_text_size</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:drawablePadding">@dimen/space_big</item>
        <item name="android:layout_marginLeft">@dimen/space_big</item>
        <item name="android:layout_marginTop">@dimen/space_big</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
